name: snapshot

on:
  workflow_dispatch:
  push:
    branches: [master]

jobs:
  preflight:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Run local verify
        run: ./scripts/verify.sh

